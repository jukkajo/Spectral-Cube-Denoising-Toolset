name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: make clean
      - run: make
      - run: make test
      - run: make sanitize
      - run: git diff --check
